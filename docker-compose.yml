version: "3.8"

volumes:
  pgsql-final-data:

services:
  postgresql:
    image: postgres:latest
    restart: unless-stopped
    container_name: pgsql-final-project
    env_file:
      - .env
    ports:
      - "5432:5432"
    volumes:
      - type: volume
        source: pgsql-final-data
        target: /var/lib/postgresql/data
